---
wave: 1
depends_on: []
files_modified:
  - beamsync/server.go
autonomous: true
requirements:
  - EVT-01
  - EVT-03
---

# Plan 1.1: Event Dispatcher Hardening

## Tasks

### Task 1.1.1 — Resize eventDispatcher buffer

<read_first>
- beamsync/server.go
</read_first>

Change `eventDispatcherBufferSize` from 256 to 1024.

Steps:
1. Locate `const eventDispatcherBufferSize = 256` at `server.go:31`
2. Change to `const eventDispatcherBufferSize = 1024`
3. Verify no other code depends on the constant value (only `newEventDispatcher(eventDispatcherBufferSize)` on line 81)

<acceptance_criteria>
- `eventDispatcherBufferSize` equals 1024
- `go build ./...` passes in `beamsync/` module
- `go vet ./...` passes
</acceptance_criteria>

<verification>
- Read the constant value: `grep 'eventDispatcherBufferSize' beamsync/server.go` shows `= 1024`
- `(cd beamsync && go build ./... && go vet ./...)` passes
</verification>

---

### Task 1.1.2 — Add drop counter to eventDispatcher

<read_first>
- beamsync/server.go
</read_first>

Add atomic drop counter to `eventDispatcher` struct.

Steps:
1. Add `"sync/atomic"` to imports
2. Add `dropped atomic.Int64` field to `eventDispatcher` struct
3. In `emit()` method, increment `d.dropped.Add(1)` in the `default:` branch before `return false`
4. Add `DroppedCount() int64` method returning `d.dropped.Load()`
5. At shutdown or periodically, log non-zero drop count: `if n := d.dropped.Load(); n > 0 { fmt.Printf("⚠️ eventDispatcher dropped %d events\n", n) }`

<acceptance_criteria>
- `eventDispatcher` has `dropped atomic.Int64` field
- `emit()` increments counter in default branch before returning false
- `DroppedCount()` method returns current drop count
- `go build ./...` passes
- `go vet ./...` passes
</acceptance_criteria>

<verification>
- `(cd beamsync && go build ./... && go vet ./...)` passes
- Verify increment is before return (not after)
</verification>

---

## Verification

```bash
(cd beamsync && go build ./... && go vet ./...)
```

## must_haves

truths:
  - "eventDispatcher buffer is 1024 (not 256)"
  - "eventDispatcher has atomic.Int64 dropped field"
  - "run() logs non-zero drop count"
  - "emit() increments dropped counter on buffer-full before returning false"
  - "DroppedCount() returns the current dropped count"
