---
phase: 01-foundation-event-system-version
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - desktop/frontend/src/App.svelte
  - desktop/frontend/src/design-system/TopNavBar.svelte
  - desktop/frontend/src/design-system/DesignSystemShowcase.svelte
autonomous: true
requirements:
  - VER-03
user_setup: []
must_haves:
  truths:
    - App.svelte fetches version from Wails bridge via window.go.main.App.GetVersion()
    - App.svelte passes dynamic appVersion to TopNavBar (not hardcoded "v2.2")
    - App.svelte About view shows dynamic appVersion (not hardcoded "v2.2")
    - TopNavBar.svelte default appVersion is "v2.4.0" (not "v2.2")
    - DesignSystemShowcase.svelte appVersion is "v2.4.0" (not "v2.2")
    - All 4 instances from D-04 are updated: App.svelte x2, TopNavBar.svelte, DesignSystemShowcase.svelte
  artifacts:
    - App.svelte with onMount GetVersion call and dynamic appVersion
    - TopNavBar.svelte with default "v2.4.0"
    - DesignSystemShowcase.svelte with "v2.4.0"
  key_links:
    - GetVersion Wails binding must exist at compile time (added in Plan 02)
    - Fallback string "v2.4.0" must match the actual wails.json productVersion (per D-03)
---

<objective>
Update all Svelte frontend files to display the version from the Wails bridge instead of hardcoded strings.

**Purpose:** Eliminate version drift by consuming the version from `wails.json` through the Go Wails binding, rather than maintaining separate hardcoded strings in the frontend.
**Output:** Modified `App.svelte` (dynamic GetVersion), `TopNavBar.svelte` (updated default), `DesignSystemShowcase.svelte` (updated version).
</objective>

<execution_context>
@/home/pranav/.config/opencode/gsd-core/workflows/execute-plan.md
@/home/pranav/.config/opencode/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-event-system-version/CONTEXT.md
@.planning/phases/01-foundation-event-system-version/01-RESEARCH.md
@desktop/frontend/src/App.svelte
@desktop/frontend/src/design-system/TopNavBar.svelte
@desktop/frontend/src/design-system/DesignSystemShowcase.svelte
</context>

<tasks>

<task type="auto">
<name>Task 1: Add dynamic GetVersion call in App.svelte</name>
<files>desktop/frontend/src/App.svelte</files>
<read_first>desktop/frontend/src/App.svelte:1-48 (script imports and setup), :254-277 (onMount where GetVersion call goes), :940-948 (TopNavBar appVersion prop), :1255-1269 (About view version badge)</read_first>
<action>
Per VER-03 and D-04 (CONTEXT.md):

1. **Add appVersion variable** in the script section. After the existing state declarations (e.g., after `let mode = "RECEIVE";` around line 50), add:
   ```js
   let appVersion = 'v2.4.0';
   ```
   The initial value is the fallback — it matches the current version per D-03, so the brief display before the async call resolves is correct.

2. **Add GetVersion call in onMount**. In the existing `onMount(async () => {` block (line 255), after `EventsOffAll();` and before the transferStatsTimer, add:
   ```js
   try {
       appVersion = await window.go.main.App.GetVersion();
   } catch {}
   ```
   The try-catch silently keeps the fallback if the Wails binding isn't available (e.g., during dev in a browser).

3. **Update TopNavBar appVersion prop** (line 944). Change:
   ```svelte
   appVersion="v2.2"
   ```
   to:
   ```svelte
   appVersion={appVersion}
   ```
   This passes the dynamic variable instead of a hardcoded string.

4. **Update About section version badge** (line 1260). Change:
   ```svelte
   <span class="version-badge">v2.2</span>
   ```
   to:
   ```svelte
   <span class="version-badge">{appVersion}</span>
   ```
   This displays the dynamic version in the About view.

Do NOT modify or remove the existing `appVersion="v2.2"` on TopNavBar.svelte's export default — that is handled in Task 2 (the TopNavBar component's own default prop).
</action>
<verify>
<automated>grep -c 'window.go.main.App.GetVersion()' desktop/frontend/src/App.svelte && grep -c 'appVersion={appVersion}' desktop/frontend/src/App.svelte && grep -c '{appVersion}' desktop/frontend/src/App.svelte</automated>
</verify>
<done>App.svelte imports GetVersion in onMount; TopNavBar uses {appVersion}; About badge uses {appVersion}</done>
<acceptance_criteria>App.svelte has `let appVersion = 'v2.4.0'`; onMount calls `window.go.main.App.GetVersion()`; TopNavBar prop changed to `appVersion={appVersion}`; About badge changed to `{appVersion}`</acceptance_criteria>
</task>

<task type="auto">
<name>Task 2: Update version defaults in TopNavBar.svelte and DesignSystemShowcase.svelte</name>
<files>desktop/frontend/src/design-system/TopNavBar.svelte, desktop/frontend/src/design-system/DesignSystemShowcase.svelte</files>
<read_first>desktop/frontend/src/design-system/TopNavBar.svelte:36 (export let appVersion = 'v2.2'), desktop/frontend/src/design-system/DesignSystemShowcase.svelte:73 (appVersion="v2.2")</read_first>
<action>
Per D-04: Update ALL remaining hardcoded "v2.2" instances in these two files.

**In `TopNavBar.svelte`** (line 36):
Change the default export:
```js
export let appVersion    = 'v2.2';
```
to:
```js
export let appVersion    = 'v2.4.0';
```

Also update the JSDoc comment on line 11 and example usage on line 23 if they reference "v2.2":
- Line 11: `appVersion     {string}  — App version string e.g. "v2.2"` → `appVersion     {string}  — App version string e.g. "v2.4.0"`
- Line 23: `appVersion="v2.2"` → `appVersion="v2.4.0"`

**In `DesignSystemShowcase.svelte`** (line 73):
Change the TopNavBar instance's prop:
```svelte
appVersion="v2.2"
```
to:
```svelte
appVersion="v2.4.0"
```

This updates the show/debug version (the DesignSystemShowcase is a development-only page).

Self-check: After this task, there should be zero instances of `"v2.2"` in the design-system directory:
```bash
grep -c '"v2\\.2"' desktop/frontend/src/design-system/
```
Should return 0.
</action>
<verify>
<automated>grep -c '"v2\\.2"' desktop/frontend/src/design-system/TopNavBar.svelte && grep -c '"v2\\.2"' desktop/frontend/src/design-system/DesignSystemShowcase.svelte</automated>
</verify>
<done>TopNavBar.svelte default changed to 'v2.4.0'; DesignSystemShowcase.svelte prop changed to "v2.4.0"; grep for '"v2\\.2"' in design-system/ returns 0</done>
<acceptance_criteria>TopNavBar.svelte export default changed to `'v2.4.0'`; DesignSystemShowcase.svelte TopNavBar prop changed to `"v2.4.0"`; doc comments updated; no "v2.2" remains in design-system/ files</acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Wails bridge→Svelte | GetVersion() return value crosses from Go (compiled) to JS (runtime) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Spoofing | GetVersion() return overwrite | low | accept | Version is display-only; no integrity requirement. If the JS runtime overwrites the value, the About view shows wrong text — no harm. |
</threat_model>

<verification>
1. Check no "v2.2" remains in the frontend source:
   ```bash
   grep -rn '"v2\\.2"' desktop/frontend/src/ --include="*.svelte"
   ```
   Should return zero matches.
2. Verify that `window.go.main.App.GetVersion()` is referenced in App.svelte:
   ```bash
   grep -c 'GetVersion' desktop/frontend/src/App.svelte
   ```
   Should return 1+.
3. No Svelte build errors (if Node tooling is available):
   ```bash
   (cd desktop/frontend && npm run check 2>/dev/null || true)
   ```
</verification>

<success_criteria>
- App.svelte fetches version from Wails bridge via GetVersion() in onMount
- TopNavBar receives dynamic {appVersion} from App.svelte
- TopNavBar.svelte default is "v2.4.0"
- DesignSystemShowcase.svelte uses "v2.4.0"
- All 4 instances from D-04 are updated: App.svelte (prop + badge), TopNavBar (default), DesignSystemShowcase (prop)
- Zero "v2.2" references remain in the Svelte source
</success_criteria>

<output>
Create `.planning/phases/01-foundation-event-system-version/01-03-SUMMARY.md` when done
</output>
