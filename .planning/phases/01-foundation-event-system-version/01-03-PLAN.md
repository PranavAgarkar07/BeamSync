---
wave: 1
depends_on: []
files_modified:
  - desktop/frontend/src/App.svelte
  - desktop/frontend/src/design-system/TopNavBar.svelte
  - desktop/frontend/src/design-system/DesignSystemShowcase.svelte
autonomous: true
requirements:
  - VER-03
---

# Plan 1.3: Frontend Version Bridge

## Tasks

### Task 1.3.1 — Dynamic version in App.svelte

<read_first>
- desktop/frontend/src/App.svelte (lines ~940-945, ~1255-1265)
</read_first>

Replace hardcoded `"v2.2"` with dynamic `GetVersion()` call.

Steps:
1. In `<script>` block, add `onMount` import if not present, then:
   ```js
   let appVersion = 'v2.4.0';
   onMount(async () => {
       try {
           appVersion = await window.go.main.App.GetVersion();
       } catch { /* fallback already set */ }
   });
   ```
2. Change `appVersion="v2.2"` on line 944 to `{appVersion}`
3. Change `<span class="version-badge">v2.2</span>` on line 1260 to `<span class="version-badge">{appVersion}</span>`

<acceptance_criteria>
- No hardcoded `"v2.2"` remains in App.svelte
- `appVersion` is initialized to `'v2.4.0'` as default
- `onMount` calls `window.go.main.App.GetVersion()` and updates
</acceptance_criteria>

<verification>
- `grep '"v2\\.2"' desktop/frontend/src/App.svelte` returns no matches
- `grep 'GetVersion' desktop/frontend/src/App.svelte` matches
</verification>

---

### Task 1.3.2 — Update default versions in shared components

<read_first>
- desktop/frontend/src/design-system/TopNavBar.svelte
- desktop/frontend/src/design-system/DesignSystemShowcase.svelte
</read_first>

Update all hardcoded version defaults.

Steps:
1. `TopNavBar.svelte:36` — change `appVersion = 'v2.2'` to `appVersion = 'v2.4.0'`
2. `TopNavBar.svelte:23` — update comment from `"v2.2"` to `"v2.4.0"`
3. `DesignSystemShowcase.svelte:73` — change `appVersion="v2.2"` to `appVersion="v2.4.0"`

<acceptance_criteria>
- No hardcoded `"v2.2"` remains in any Svelte file under `desktop/frontend/src/`
</acceptance_criteria>

<verification>
- `grep -rn '"v2\\.2"' desktop/frontend/src/` returns zero matches
</verification>

---

## Verification

```bash
grep -rn '"v2\.2"' desktop/frontend/src/  # should return nothing
```

## must_haves

truths:
  - "App.svelte dynamically calls GetVersion() on mount"
  - "No hardcoded 'v2.2' version strings remain in any Svelte file"
  - "TopNavBar.svelte default appVersion is 'v2.4.0'"
  - "DesignSystemShowcase.svelte appVersion is 'v2.4.0'"
